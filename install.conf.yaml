- clean: ['~']

- link:
    ~/.vimrc: vimrc
    ~/.zshenv: zshenv
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.ssh/config:
        path: ssh/config
        create: true


- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf


- clean: ['~']

- link:


